local options = {
  ensure_installed = { "lua", "c", "python", "bash", "make" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
