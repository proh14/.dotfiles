local options = {
  ensure_installed = { "lua", "c" , "bash" , "make" , "vim" , "nvim"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
